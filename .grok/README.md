# Project Grok Content

This directory contains Grok-related content that is shipped with the portable
application. The bundled `skills/abya-game-development-task` directory is
deployed into every task workspace so Grok can discover the ABYA workflow from
that task's current working directory.

Do not store authentication files, user sessions, logs, caches, API keys, or
other user-specific state here.
